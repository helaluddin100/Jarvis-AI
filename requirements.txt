# Jarvis - Personal AI Assistant (Free stack)

# AI / LLM - Ollama (local) or Groq (cloud free tier)
requests>=2.31.0
httpx>=0.25.0

# Speech: listen (mic -> text)
SpeechRecognition>=3.10.0
pyaudio>=0.2.14

# Voice verification – শুধু তোমার ভয়েসে কাজ (অন্য ভয়েসে PC স্লিপ)
resemblyzer>=0.1.4
numpy>=1.20.0
# Optional: openai-whisper for local STT (uncomment if using)
# openai-whisper>=20231117

# Speech: speak (text -> voice) - offline + Bangla (gTTS)
pyttsx3>=2.90
gTTS>=2.4.0

# PC control
pyautogui>=0.9.54
keyboard>=0.13.5
# Optional: for more system control on macOS
# pyobjc-framework-Quartz>=10.0

# Utilities
python-dotenv>=1.0.0
